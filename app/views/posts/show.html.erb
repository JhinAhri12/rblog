<h2><%= @post.title %></h2>
<p><%= @post.body %></p>

<%= link_to "Editer le post", edit_post_path(@post) %>
<%= link_to "Delete",
  post_path(@post),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<div>
  <%= link_to "Back to posts", posts_path %>
</div>